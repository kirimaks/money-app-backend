scalar Timestamp

type TransactionResponse { 
  name: String!
  amount: Float!
  timestamp: Timestamp!
  id: String!
}

type Mutation {
  createTransaction(name:String! amount:Float! timestamp:Timestamp!): TransactionResponse!
}
