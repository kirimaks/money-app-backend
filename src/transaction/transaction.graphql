scalar Timestamp

type TransactionResponse { 
  name: String!
  amount: Float!
  timestamp: Timestamp!
  id: String!
  categoryId: String
}

type Query {
  transaction(id:String!): TransactionResponse!
}

type Mutation {
  createTransaction(name:String! amount:Float! timestamp:Timestamp! categoryId:String): TransactionResponse!
}
