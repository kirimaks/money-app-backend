type Account {
  name: String!
}

type ProfileResponse {
    user: User!
    account: Account!
}

type Query {
    profile: ProfileResponse!
}

type Mutation {
    updateProfile(firstName: String lastName: String): ProfileResponse!
}
